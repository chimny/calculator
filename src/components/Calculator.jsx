import {Keyboard} from "./Keyboard";

export const Calculator = () => {

    return (

        <div>
            <input type="text"  />
            <br/>
            <Keyboard/>
        </div>

    )
}